<AuButton @skin="naked" class="c-favorite {{if this.isBookmarked "is-active"}}" {{on "click" this.toggleFavorite}}>
  <AuIcon @icon={{if this.isBookmarked "star-filled" "star-outline"}} />
  <span class="au-u-hidden-visually">Favoriet</span>
</AuButton>